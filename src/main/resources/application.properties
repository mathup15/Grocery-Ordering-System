# --- SQL Server ---
spring.datasource.url=jdbc:sqlserver://localhost:1433;databaseName=ordersystem;encrypt=false;trustServerCertificate=true
spring.datasource.username=Mathu
spring.datasource.password=mathu2003
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServerDialect
# --- existing DB/JPA lines stay as-is ---

# JWT
app.jwt.secret=CHANGE_ME_TO_A_LONG_RANDOM_SECRET_32+_CHARS_1234567890
app.jwt.exp-hours=8

# ?keep your existing SQL Server + JPA settings?

# where images will be stored (relative to project run dir unless absolute)
app.upload-dir=uploads

# file size limits (tweak as you like)
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=6MB


